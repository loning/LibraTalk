using System;
using System.Threading.Tasks;
using LibraProgramming.Grains.Interfaces;
using Orleans;

namespace LibraProgramming.Grains.Implementation
{
/*
    public class DateTimeGrain : IDateTimeGrain
    {
        public Task<DateTime> GetTimeAsync()
        {
            var key = this.GetPrimaryKeyString().ToLowerInvariant();

            switch (key)
            {
                case "server":
                {
                    return Task.FromResult(DateTime.UtcNow);
                }

                default:
                {
                    return Task.FromResult(DateTime.Now);
                }
            }
        }
    }
*/
}